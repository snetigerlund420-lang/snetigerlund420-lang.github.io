source "https://rubygems.org"

# Using the github-pages gem locks Jekyll + plugins to exactly what
# GitHub Pages runs in production, so "works on my machine" == "works live".
gem "github-pages", group: :jekyll_plugins

# Only needed for local preview (`bundle exec jekyll serve`).
# GitHub's own build does not use this Gemfile at all.
